class LLMService {
  // TODO: Connect local LLM and optional remote inference.
  Future<String> generateResponse(String prompt) async {
    throw UnimplementedError('LLM inference is not connected yet.');
  }
}
