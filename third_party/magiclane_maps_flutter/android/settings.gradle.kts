pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

plugins {
    id("com.android.library") version "8.13.0" apply false
}

rootProject.name = "magiclane_maps_flutter"
rootProject.buildFileName = "build.gradle.kts"
